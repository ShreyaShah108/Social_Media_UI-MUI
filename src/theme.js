import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette:{
        primary: {
        main: "#00ADB5",
        light: "#AAD8D3"
        },
        secondary: {
            main: "#64CCC5"
        },
        textColor: {
            main: "#393E46"
        }
    }
})